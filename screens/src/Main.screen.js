import { Text } from 'components/export'
import SafeAreaView from 'utils/SafeAreaView'

export default function MainScreen() {
  return (
    <SafeAreaView mode={'dark'}>
      <Text content={'main'} />
    </SafeAreaView>
  )
}
