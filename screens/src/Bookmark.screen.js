import { Text } from 'components/export'
import AreaView from 'utils/TabAreaView'

export default function BookmarkScreen() {
  return (
    <AreaView mode={'dark'}>
      <Text content={'bookmarks'} />
    </AreaView>
  )
}
